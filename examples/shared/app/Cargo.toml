[package]
name = "app"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
mylib = { path = "../mylib", artifact = "cdylib", target = "wasm32-wasip1", lib = true }
offload = { path = "../../../offload" }
