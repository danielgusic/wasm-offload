[package]
name = "mylib"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
offload = { path = "../../../offload" }
serde = { version = "1.0.229", features = ["derive"] }
